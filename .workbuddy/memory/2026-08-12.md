# 2026-08-12

## P6.2 Student profile + session context — 完成

- 给 `Student` 表加了 `focus_text_types` 字段（JSON-encoded list，自定义 `StringList` TypeDecorator）
- `database.init_db()` 加了幂等迁移：`_ensure_student_focus_text_types_column()`
- 新增 API：`POST/GET /api/students`、`GET/PATCH /api/students/{id}`
- `StartSessionRequest.student_id` 可选；设置后 `InteractiveLoop._resolve_student()` 加载 profile，`_resolve_text_type()` 让 `focus_text_types[0]` 覆盖 request 的 `text_type`
- `_base_inputs()` 从 student row 取 year_level + text_type，保证 reload 后一致
- 前端：新 `ProfileView.tsx`（create/edit/saved）、App.tsx 加 Profile tab、ChatView 显示当前 profile、storage.ts 持久化 profile
- 9 个新测试；pytest 131 passed + 4 skipped；ruff/mypy/tsc/vite 全绿；oxlint 0 errors

## 工具笔记

- 后端 venv 在 `backend/.venv/Scripts/`，运行 pytest 用 `./.venv/Scripts/python.exe -m pytest`
- Windows temp 权限问题：需要 `TMP=TEMP=backend/.tmp` 环境变量 + `-p no:cacheprovider`
- 前端 npm 在 `C:/Users/miuid/AppData/Local/Programs/kimi-desktop/resources/resources/runtime/npm.cmd`
- vite build 在沙箱里清空 dist 会失败（safe-delete 错误），需要 `rm -rf dist` 后重试（可能要 escalation）
